@model IEnumerable<Inventarsystem1.Models.Seriennummer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Srnummer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SeriennummerNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Srnummer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SeriennummerNavigation.ProduktId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SeriennummerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SeriennummerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SeriennummerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
